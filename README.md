# Payment notifier

Play a sounds when a payment is done

## Binary

```
make binary-build

cp ./.env.dist ./.env

./bin/notifier
```

## Docker

```
make docker-build

cp ./.env.dist ./.env

./bin/notifier
```

